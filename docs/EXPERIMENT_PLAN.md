# Experiment Plan

This plan records the intended experiments and the current completion status. It distinguishes the official default GenImage Split B from the fallback available-generator split that was actually run.

## 1. CIFAKE Full Sanity Check

Status: completed.

Purpose: verify the full training and evaluation pipeline.

Completed experiment:

- `cifake_full_resnet50`
- Checkpoint: `outputs/checkpoints/cifake_full_resnet50/best_model.pt`
- Metrics: `outputs/metrics/cifake_full_resnet50_metrics.csv`
- Predictions: `outputs/predictions/cifake_full_resnet50_predictions.csv`
- Figures: `outputs/figures/cifake_full_resnet50/`

## 2. Official Default GenImage Split B

Status: pending.

The official default Split B was not run because the local GenImage subset is missing `Stable Diffusion V1.4`.

Configured default generators:

- Train: `Stable Diffusion V1.4`, `ADM`, `BigGAN`
- Unseen test: `GLIDE`, `Midjourney`, `Wukong`, `VQDM`

Next action for this track: download the missing `Stable Diffusion V1.4` subset, then regenerate the official Split B CSVs.

## 3. GenImage Fallback Available-Generator Split

Status: completed.

This fallback split was used because `Stable Diffusion V1.4` is missing.

Actual generators:

- Train: `Stable Diffusion V1.5`, `ADM`, `BigGAN`
- Unseen test: `GLIDE`, `Midjourney`, `Wukong`, `VQDM`

Completed experiments:

- `genimage_splitB_available_resnet50`
- `genimage_splitB_available_clip_mlp`

Key files:

- Train split: `outputs/splits/genimage_splitB_available_train.csv`
- Validation split: `outputs/splits/genimage_splitB_available_val.csv`
- Unseen test split: `outputs/splits/genimage_splitB_available_test_unseen.csv`
- Metrics: `outputs/metrics/genimage_splitB_available_resnet50_metrics.csv`
- Metrics: `outputs/metrics/genimage_splitB_available_clip_mlp_metrics.csv`
- Checkpoint: `outputs/checkpoints/genimage_splitB_available_resnet50/best_model.pt`
- Checkpoint: `outputs/checkpoints/genimage_splitB_available_clip_mlp/best_model.pt`

## 4. Post-Processing Robustness

Status: completed.

Purpose: compare clean AUROC with AUROC after common post-processing transforms.

Completed robustness evaluations:

- `genimage_splitB_available_resnet50_robustness`
- `genimage_splitB_available_clip_mlp_robustness`

Transforms:

- clean
- JPEG compression
- resize down-up
- Gaussian blur

Key files:

- `outputs/metrics/genimage_splitB_available_resnet50_robustness_robustness.csv`
- `outputs/metrics/genimage_splitB_available_clip_mlp_robustness_robustness.csv`
- `outputs/figures/genimage_splitB_available_resnet50_robustness/robustness_bar_chart.png`
- `outputs/figures/genimage_splitB_available_clip_mlp_robustness/robustness_bar_chart.png`

## 5. Defactify External Evaluation

Status: implementation complete; dataset preparation and experiments pending.

Defactify replaces WildFake as the current external cross-dataset test. It uses the official pinned `test` split for the primary result, a balanced test manifest for class-balance diagnostics, and Label_B for per-generator analysis. It is never used to train the GenImage checkpoints.

Planned experiments:

- `defactify_external_available_resnet50`
- `defactify_external_available_clip_mlp`
- `defactify_balanced_available_resnet50`
- `defactify_balanced_available_clip_mlp`
- `defactify_smoke_resnet50`
- `defactify_smoke_clip_mlp`

Preparation must record a verified Hugging Face commit SHA in `DEFACTIFY_REVISION`.

## 6. WildFake Subset External Evaluation (Milestone 2 legacy)

Status: completed for subset only.

This is not full WildFake. The completed evaluation used a WildFake subset consisting of real images from CelebA-HQ and fake images generated by DDIM.

Completed experiments:

- `wildfake_subset_external_available_resnet50`
- `wildfake_subset_external_available_clip_mlp`

Key files:

- Inventory: `outputs/inventories/wildfake_inventory.csv`
- Main subset split: `outputs/splits/wildfake_subset_baseline_test.csv`
- Metrics: `outputs/metrics/wildfake_subset_external_available_resnet50_metrics.csv`
- Metrics: `outputs/metrics/wildfake_subset_external_available_clip_mlp_metrics.csv`
- Figures: `outputs/figures/wildfake_subset_external_available_resnet50/`
- Figures: `outputs/figures/wildfake_subset_external_available_clip_mlp/`

## 7. Full WildFake External Evaluation (legacy, optional)

Status: optional / pending.

Full WildFake has not been run. Future work can add more WildFake sources and generators or run the full dataset if it becomes available locally.

## 8. Reporting Outputs

Status: completed.

Report-ready summary files:

- `outputs/metrics/summary_all_experiments.csv`
- `docs/RESULTS_SUMMARY.md`
